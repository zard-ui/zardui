# Convention

The `background` / `foreground` pairing, in OKLCH — the format ZardUI ships.
Values come from the `neutral` preset in `packages/cli/src/core/themes/theme-definitions.ts`.

```css
--primary: oklch(0.205 0 0);
--primary-foreground: oklch(0.985 0 0);
```
