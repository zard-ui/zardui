```bash tab="npm" copyButton
npx zard-cli@latest add calendar
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add calendar
```

```bash tab="yarn"
yarn dlx zard-cli@latest add calendar
```

```bash tab="bun"
bunx zard-cli@latest add calendar
```