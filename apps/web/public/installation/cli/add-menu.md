```bash tab="npm" copyButton
npx zard-cli@latest add menu
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add menu
```

```bash tab="yarn"
yarn dlx zard-cli@latest add menu
```

```bash tab="bun"
bunx zard-cli@latest add menu
```