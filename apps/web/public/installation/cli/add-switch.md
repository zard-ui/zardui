```bash tab="npm" copyButton
npx zard-cli@latest add switch
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add switch
```

```bash tab="yarn"
yarn dlx zard-cli@latest add switch
```

```bash tab="bun"
bunx zard-cli@latest add switch
```