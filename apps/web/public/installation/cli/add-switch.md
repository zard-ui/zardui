```bash tab="npm" copyButton
npx @ngzard/ui@latest add switch
```

```bash tab="pnpm"
pnpm dlx @ngzard/ui@latest add switch
```

```bash tab="yarn"
yarn dlx @ngzard/ui@latest add switch
```

```bash tab="bun"
bunx @ngzard/ui@latest add switch
```