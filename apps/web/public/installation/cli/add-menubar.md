```bash tab="npm" copyButton
npx zard-cli add menubar
```

```bash tab="pnpm"
pnpm dlx zard-cli add menubar
```

```bash tab="yarn"
yarn dlx zard-cli add menubar
```

```bash tab="bun"
bunx zard-cli add menubar
```