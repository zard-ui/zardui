```bash tab="npm" copyButton
npx zard-cli@latest add input-group
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add input-group
```

```bash tab="yarn"
yarn dlx zard-cli@latest add input-group
```

```bash tab="bun"
bunx zard-cli@latest add input-group
```