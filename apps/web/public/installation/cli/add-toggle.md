```bash tab="npm" copyButton
npx zard-cli@latest add toggle
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add toggle
```

```bash tab="yarn"
yarn dlx zard-cli@latest add toggle
```

```bash tab="bun"
bunx zard-cli@latest add toggle
```