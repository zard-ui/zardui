```bash tab="npm" copyButton
npx zard-cli@latest add tabs
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add tabs
```

```bash tab="yarn"
yarn dlx zard-cli@latest add tabs
```

```bash tab="bun"
bunx zard-cli@latest add tabs
```