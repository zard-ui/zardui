```bash tab="npm" copyButton
npx zard-cli@latest add skeleton
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add skeleton
```

```bash tab="yarn"
yarn dlx zard-cli@latest add skeleton
```

```bash tab="bun"
bunx zard-cli@latest add skeleton
```