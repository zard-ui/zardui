```bash tab="npm" copyButton
npx zard-cli@latest add date-picker
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add date-picker
```

```bash tab="yarn"
yarn dlx zard-cli@latest add date-picker
```

```bash tab="bun"
bunx zard-cli@latest add date-picker
```