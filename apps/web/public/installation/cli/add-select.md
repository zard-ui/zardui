```bash tab="npm" copyButton
npx zard-cli@latest add select
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add select
```

```bash tab="yarn"
yarn dlx zard-cli@latest add select
```

```bash tab="bun"
bunx zard-cli@latest add select
```