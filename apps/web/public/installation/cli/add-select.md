```bash tab="npm" copyButton
npx @ngzard/ui@latest add select
```

```bash tab="pnpm"
pnpm dlx @ngzard/ui@latest add select
```

```bash tab="yarn"
yarn dlx @ngzard/ui@latest add select
```

```bash tab="bun"
bunx @ngzard/ui@latest add select
```