```bash tab="npm" copyButton
npx zard-cli@latest add badge
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add badge
```

```bash tab="yarn"
yarn dlx zard-cli@latest add badge
```

```bash tab="bun"
bunx zard-cli@latest add badge
```