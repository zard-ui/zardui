```bash tab="npm" copyButton
npx zard-cli@latest add carousel
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add carousel
```

```bash tab="yarn"
yarn dlx zard-cli@latest add carousel
```

```bash tab="bun"
bunx zard-cli@latest add carousel
```