```bash tab="npm" copyButton
npx @ngzard/ui@latest add loader
```

```bash tab="pnpm"
pnpm dlx @ngzard/ui@latest add loader
```

```bash tab="yarn"
yarn dlx @ngzard/ui@latest add loader
```

```bash tab="bun"
bunx @ngzard/ui@latest add loader
```