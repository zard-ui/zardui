```bash tab="npm" copyButton
npx zard-cli@latest add loader
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add loader
```

```bash tab="yarn"
yarn dlx zard-cli@latest add loader
```

```bash tab="bun"
bunx zard-cli@latest add loader
```