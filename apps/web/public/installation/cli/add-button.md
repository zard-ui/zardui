```bash tab="npm" copyButton
npx @ngzard/ui@latest add button
```

```bash tab="pnpm"
pnpm dlx @ngzard/ui@latest add button
```

```bash tab="yarn"
yarn dlx @ngzard/ui@latest add button
```

```bash tab="bun"
bunx @ngzard/ui@latest add button
```