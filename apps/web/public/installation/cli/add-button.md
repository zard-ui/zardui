```bash tab="npm" copyButton
npx zard-cli@latest add button
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add button
```

```bash tab="yarn"
yarn dlx zard-cli@latest add button
```

```bash tab="bun"
bunx zard-cli@latest add button
```