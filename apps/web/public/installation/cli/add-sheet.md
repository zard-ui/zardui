```bash tab="npm" copyButton
npx zard-cli@latest add sheet
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add sheet
```

```bash tab="yarn"
yarn dlx zard-cli@latest add sheet
```

```bash tab="bun"
bunx zard-cli@latest add sheet
```