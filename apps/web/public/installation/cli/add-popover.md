```bash tab="npm" copyButton
npx zard-cli@latest add popover
```

```bash tab="pnpm"
pnpm dlx zard-cli@latest add popover
```

```bash tab="yarn"
yarn dlx zard-cli@latest add popover
```

```bash tab="bun"
bunx zard-cli@latest add popover
```