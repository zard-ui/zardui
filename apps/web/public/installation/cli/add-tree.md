```bash tab="npm" copyButton
npx @ngzard/ui@latest add tree
```

```bash tab="pnpm"
pnpm dlx @ngzard/ui@latest add tree
```

```bash tab="yarn"
yarn dlx @ngzard/ui@latest add tree
```

```bash tab="bun"
bunx @ngzard/ui@latest add tree
```