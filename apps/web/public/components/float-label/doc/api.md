## [z-float-label] <span class="api-type-label component">Component</span>

> Wraps `input`, `textarea`, or `z-select` with a floating label.  
> The label floats when the input has focus or contains a value.